[build-system]
requires = [
    "setuptools>=42",
    "wheel",
    "pybind11",
    "scikit-build",
    "cmake"
]
build-backend = "setuptools.build_meta"

[tool.black]
line-length = 100
target-version = ['py39']